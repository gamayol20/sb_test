name: 'sb_finance_project'
version: '1.0.0'
config-version: 2

profile: 'sb_finance_project'

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  sb_finance_project:
    staging:
      +materialized: view
    marts:
      +materialized: table